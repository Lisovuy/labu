#include <cstdlib>
#include <iostream>
#include <math.h>
using namespace std;
int main (int argc,char *argv[])
{
double A,C =1.231,D =1.152,x,y;
cout<<"x=";
cin>>x;
cout<<"y=";
cin>>y;
if(x>y) A=exp(y)+sqrt(C*x+y);
else A=pow(x,1/3)+D*y;
cout<<"A="<<A<<endl;
	return 0;
}
