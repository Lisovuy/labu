#include <cstdlib>
#include <iostream>
#include <math.h>
using namespace std;
int main (int argc,char *argv[])
{
	double a,b,x,y;
	cout<<"vvedit a=";
	cin>>a;
	cout<<"vvedit b=";
	cin>> b;
	cout<<"vvedit x=";
	cin>>x;
	y=pow(x,2/7)+sqrt(x)*exp(1.5)*log10(x)-acos(a)+tan(2*b);
	cout<<"y="<<y<<endl;
	return 0;
}
